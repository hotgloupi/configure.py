
language: python
python:
    - "3.2"
    - "3.3"
    - "3.4"
install:
    - pip install behave
    - python setup.py install
    - sudo apt-get install g++
script:
    - python setup.py test
    - behave tests/features -q -m
git:
    submodules: false
